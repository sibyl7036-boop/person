# 实施计划 — Sibyl 个人动态网页简历

- [ ] 1. 创建项目基础结构与数据配置文件（data.js）
   - 在 `/Users/sibyl/Desktop/person/` 下创建项目文件结构：`index.html`、`styles.css`、`data.js`、`app.js`
   - 在 `data.js` 中定义所有个人信息的数据结构，包括：Hero 区域标签列表、教育经历数组、实习经历数组（含详细成就要点）、AI 项目数组（从实习经历提炼的 6 个项目）、塔罗牌卡片数组（6 张卡片的标题/emoji/描述/趣味数据）、去过的省份列表
   - 数据结构需支持后续动态渲染，每个条目包含完整的字段定义
   - _需求：10.1、10.2、10.3、10.4、10.5_

- [ ] 2. 实现全局样式基础与 CSS 变量体系
   - 在 `styles.css` 中定义 CSS 自定义属性（变量）：主背景色 #F5F0EB、主文字色 #5C3D2E、强调色 #8B3A3A、卡片背景色 #F5E6C8、点缀色 #4A5D4A
   - 引入 Google Fonts（Playfair Display 衬线体 + Inter 无衬线体 + Noto Sans SC 中文无衬线体）
   - 实现全局 reset、基础排版样式（标题衬线体、正文无衬线体）、杂志风格的留白与间距体系
   - 定义通用的入场动画 CSS 类（fade-in、slide-up）、hover 过渡效果、响应式断点（768px / 1024px）
   - 实现 `prefers-reduced-motion` 媒体查询降级
   - _需求：1.1、1.2、1.3、1.4、1.6、8.1、8.4_

- [ ] 3. 搭建 HTML 页面骨架与导航栏
   - 在 `index.html` 中搭建完整的 HTML5 语义化结构：`<nav>`、`<section id="hero">`、`<section id="experiences">`、`<section id="projects">`、`<section id="capability">`、`<footer>`
   - 实现顶部导航栏：左侧 "Sibyl" 品牌标识（衬线体）、右侧三个导航链接（Experiences / AI Projects / Capability）
   - 导航栏 CSS：sticky 定位、半透明米白背景、下划线高亮激活态、深棕色文字
   - 引入 CDN 资源：GSAP（用于塔罗牌动画）
   - _需求：2.1、2.2、2.4、9.1_

- [ ] 4. 实现 Hero 首页区域（打字机标签效果）
   - 在 `app.js` 中实现 Hero 区域的动态渲染：大标题 "Know about Sibyl"（衬线体、淡入动画）、副标题 "王语彤 | AI Product Manager"
   - 实现打字机效果：从 `data.js` 读取标签列表，以 300-500ms 间隔逐个弹出浮动标签胶囊，标签随机分布在标题周围
   - 标签胶囊样式：米白底色 + 深棕色文字 + 细边框 / 淡黄色底色 + 暗红色文字，圆角胶囊形状
   - 实现标签的呼吸浮动动画（CSS animation）和 hover 效果（阴影变化、边框加深）
   - _需求：3.1、3.2、3.3、3.4、3.5、3.6_

- [ ] 5. 实现 Experiences 模块（时间轴展示）
   - 在 `app.js` 中实现时间轴的动态渲染：从 `data.js` 读取教育经历和实习经历数据，生成交替左右排列的时间轴条目
   - 时间轴 CSS：中央竖线（深棕色细线）、节点圆点（暗红色）、条目卡片（淡黄色背景、圆角、柔和阴影）
   - 每个条目包含：时间范围（暗红色）、机构名称（衬线体粗体）、角色描述、成就要点列表
   - 模块标题 "The Practitioner" 搭配装饰性分隔线
   - 响应式：移动端改为单列布局
   - _需求：4.1、4.2、4.3、4.4、4.6_

- [ ] 6. 实现 AI Projects 模块（杂志风格卡片展示）
   - 在 `app.js` 中实现项目卡片的动态渲染：从 `data.js` 读取 6 个项目数据，生成杂志风格的大卡片
   - 卡片布局：奇偶交替的不对称网格（奇数卡片左图右文、偶数卡片右图左文，无图时使用装饰色块/emoji 大图标替代）
   - 每个卡片包含：大号衬线体编号（"01"-"06"）、类型胶囊标签（暗红底+白字）、项目名称（衬线体）、描述段落、关键数据亮点（加粗）、技术标签胶囊
   - 模块标题 "The Creator" 搭配装饰性分隔线
   - 卡片间使用引用块装饰（大引号）或分隔线区隔
   - hover 效果：轻微上浮 + 阴影增强
   - 响应式：移动端改为单列全宽
   - _需求：5.1、5.2、5.3、5.4、5.5、5.6、5.7_

- [ ] 7. 实现 Capability 模块 — 塔罗牌扇形布局与翻转交互
   - 在 `app.js` 中实现塔罗牌模块：从 `data.js` 读取 6 张卡片数据，生成扇形排列的卡片
   - 卡片背面设计：暗红/深棕色渐变底色 + 中央星星✦装饰图案 + 圆角 + 柔和阴影
   - 扇形布局：6 张卡片以中心点为基准，每张旋转不同角度（约 -15° 到 +15°），形成扇形散开效果
   - 使用 GSAP 实现点击翻转动画：卡片 3D 翻转（rotateY 180°）+ 移动到屏幕中央 + 放大展示
   - 卡片正面展示各兴趣爱好的详细内容（标题、emoji、描述文字、趣味数据）
   - 关闭/收回动画：卡片缩小翻回背面，回到扇形原始位置
   - hover 效果：未翻开卡片轻微上浮 + 边框发光
   - 模块标题 "The Explorer" + 引导提示 "✦ Click cards to explore my DNA ✦"
   - _需求：6.1、6.2、6.3、6.4、6.5、6.6、6.7_

- [ ] 8. 实现旅行者卡片中的中国地图 SVG
   - 创建 `china-map.js`，使用内联 SVG path 数据绘制简化版中国地图（各省份轮廓）
   - 从 `data.js` 读取去过的省份列表，为对应省份 path 填充暖色（淡粉、淡橙、淡黄等不同颜色），未去过的省份填充浅灰色
   - 地图整体风格可爱简洁，带有圆润的边角处理
   - 鼠标悬停省份时显示省份名称 tooltip
   - 将地图组件集成到塔罗牌"旅行者"卡片的正面内容中
   - _需求：6.5（旅行者卡片）、6.8、10.5_

- [ ] 9. 实现滚动交互、导航高亮与入场动画系统
   - 在 `app.js` 中使用 Intersection Observer API 实现：元素进入视口时触发入场动画（淡入 + 轻微上移，0.6-0.8s）
   - 实现导航栏滚动高亮：监听各 section 的可见性，自动更新导航栏激活态（下划线高亮）
   - 实现平滑滚动：导航点击时 smooth scroll 到对应模块
   - 实现塔罗牌全部翻开后的彩蛋动画（撒花效果或温馨提示文字）
   - 实现页脚区域："Made with ♥ by Sibyl" + 社交图标（使用 Unicode/SVG 图标）
   - _需求：2.2、2.3、7.1、7.2、7.3、8.1、8.2、8.3、6.9_

- [ ] 10. 响应式适配与最终优化
   - 完善所有模块的移动端响应式布局：时间轴单列、项目卡片全宽、塔罗牌缩小扇形角度、Hero 标签重排
   - 优化图片懒加载策略（如有图片资源）
   - 测试 `prefers-reduced-motion` 降级效果
   - 确保首屏渲染 < 3s，动画保持 60fps
   - 创建 Dockerfile（nginx 静态文件服务器）
   - _需求：1.5、4.6、5.7、8.4、8.5、9.2、9.4、9.5、9.6_
